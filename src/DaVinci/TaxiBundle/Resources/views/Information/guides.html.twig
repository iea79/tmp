{% extends 'DaVinciTaxiBundle::base.html.twig' %}
{% block theme 'instructions' %}
{% block title %}
    {% include 'DaVinciTaxiBundle:Banners:banner1.html.twig' %}
{% endblock%}
{% block body %}
{# ======= Swicher button ========= #}
	<div class="shadow-main whitebg">
        <div class="buttom-swicher">
            <ul class="uk-width-1-1">
               <li{% if category == 'passenger' %} class="uk-active"{% endif %}>
                   <a href="{{ path('guides') }}">{{ 'For passengers'|trans }}</a>
               </li>
               <li{% if category == 'driver' %} class="uk-active"{% endif %}>
                   <a href="{{ path('guides', {'category': 'driver'}) }}">{{ 'For drivers'|trans }}</a>
               </li>
            </ul>
        </div>
        <div class="message">
        {#
        <span class="uk-text-danger">{{'Внимание!'|trans}}</span> {{'Не забывайте подтвердить вашу готовность выполнить заказ.'|trans}}
        #}
        </div>
        <div class="search">
            <form class="uk-search" data-uk-search>
                <input class="uk-search-field" type="search" id="filter" placeholder="Search text">
            </form>
        </div>
		<ul id="help" class="uk-switcher">
            <li class="uk-active">
				<div class="block-help">
					<div class="help-content">
						<div class="buttom-swicher">
				            <ul class="uk-width-1-1">
                               <li class="uk-active">
                                   <a href="#">Category 1</a>
                               </li>
                               <li class="">
                                   <a href="#">Category 1</a>
                               </li>
				            </ul>
				        </div>
						<ul class="uk-nav only-link">           
                            {% for guideItem in guides %}
                                <li>
                                    <a href="{{ path('guide', {'contentId': guideItem}) }}">{{ guideItem.title }}</a>
                                </li> 
                            {% endfor %}
                        </ul>
				    	{# 
                        <a href="#" class="uk-button uk-width-1-1 see-all">{{'See all'|trans}}(28)</a> 
                        #}
				    	<div class="support">
				    		<span>{{ "Can't find your answer here? Submit a support request."|trans }}</span>
				    		<a href="" class="uk-button uk-button-success">{{ 'GET TMP SUPPORT'|trans }}</a>
				    	</div>
					</div>
					<div class="help-sidebar">
						<ul class="uk-nav">
			                <li class="head">
                                <a href="{{ path('faqs') }}">{{ 'FAQs'|trans }}</a>
                            </li>
	                        {% for faq in faqs %}
                                {% if loop.index < 6 %}
                                    <li>
                                        <a href="{{ path('faqs') }}#{{ faq }}">{{ faq.question }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
	                        <li>
                                <a class="uk-button uk-button-primary" href="{{ path('faqs') }}">{{ 'more questions'|trans }}</a>
                            </li>
	                    </ul>
                        {#
						<ul class="uk-nav demo">
			                <li class="head"><a href="#">Demo</a></li>
	                        <li>
	                        	<a href="#" class="preview-video">
									<img src="{{ asset('bundles/davincitaxi/images/video1.jpg') }}" alt="{{ ''|trans}}" />
								</a>
							</li>
	                        <li class="link"><a href="#">Как начать пользоваться сервисом Taxi My Price</a></li>
	                        <li>
	                        	<a href="#" class="preview-video">
									<img src="{{ asset('bundles/davincitaxi/images/video2.jpg') }}" alt="{{ ''|trans}}" />
								</a>
	                        </li>
	                        <li class="link"><a href="#">Как настроить профиль пассажира в Taxi My Price</a></li>
	                        <li>
	                        	<a href="#" class="preview-video">
									<img src="{{ asset('bundles/davincitaxi/images/blog1.jpg') }}" alt="{{ ''|trans}}" />
								</a>
							</li>
	                        <li class="link"><a href="#">Как начать пользоваться сервисом Taxi My Price</a></li>
	                        <li>
	                        	<a href="#" class="preview-video">
									<img src="{{ asset('bundles/davincitaxi/images/blog2.jpg') }}" alt="{{ ''|trans}}" />
								</a>
	                        </li>
	                        <li class="link"><a href="#">Как настроить профиль пассажира в Taxi My Price</a></li>
	                        <li><a class="uk-button uk-button-primary" href="#">more demo</a></li>
	                    </ul>
                        #}
	                </div>
				</div>
			</li>
            <li class="uk-active">
				<div class="block-help">
					<div class="help-content">
						<h3>{{'Guides:'|trans}}</h3>
                        <ul class="uk-nav only-link">
                            {% for guide in otherGuides %}
                                {% if loop.index < 6 %}
                                    <li>
                                        <a href="{{ path('guide', {'contentId': guide}) }}">{{ guide.title }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
				    	{# 
                        <a href="" class="uk-button uk-width-1-1 see-all">{{'See all '|trans}}(28)</a> 
                        #}
				    	<div class="support">
				    		<span>{{ "If you don't find how to solve problem please answer our support team!"|trans }}</span>
				    		<a href="#" class="uk-button uk-button-success">
                                {{ 'GET TMP SUPPORT'|trans }}
                            </a>
				    	</div>
					</div>
                </div>
			</li>
		</ul>
	</div>
{% endblock %}

{% block javascripts %}
    {% javascripts filter='?requirejs'
    '@DaVinciTaxiBundle/Resources/public/js/pages/help/guides.js'
    %}

    {{ require_js_initialize({ 'main' : asset_url })}}

    {% endjavascripts %}
{% endblock %}