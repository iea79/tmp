{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
{% include 'DaVinciTaxiBundle::breadcrumbs.html.twig' %}

<div class="wrapper-content"><div class="content">
    {% if error %}
    <div>{{ error|trans }}</div>
    {% endif %}
    <form class="uk-form uk-form-horizontal authorizat" action="{{ path("fos_user_security_check") }}" method="post">
        <!--table class="authoriz">
            <tr>
                <td-->
        <h3>{{ 'Login or Register'|trans}}</h3>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

            <input class="uk-width-1-1" type="text" id="username" name="_username" value="{{ last_username }}" required="required" placeholder="{{ 'security.login.emailmobile'|trans }}" />
            <input class="uk-width-1-1" type="password" id="password" name="_password" required="required" placeholder ="{{ 'security.login.password'|trans }}" />
            <div class="clear"></div>
            <p class="blue"><a href="{{ path('fos_user_resetting_request') }}">{{ 'Forgot your password?'|trans }}</a></p>
			
			<button class="uk-button nextbutt uk-width-1-1" id="_submit" name="_submit">{{ 'security.login.submit'|trans }}</button>
            
			<!--input class="uk-button nextbutt" type="submit" id="_submit" name="_submit" value="" /-->
            <!--/td>
            <th>or</th>
            <td-->
			<button class="uk-button float_left nextbutt btn-red uk-width-1-1" onclick="location.href = '{{ path("fos_user_registration_register") }}';">Sign Up</button>

            <!--input type="button" class="uk-button float_left nextbutt btn-red" onclick="location.href = '{{ path("fos_user_registration_register") }}';" value="Sign Up" /-->
            <!--    <a class="loginfb" href="#"></a>
                <a class="logintw" href="#"></a>
            <h3>Sign in with</h3>
            <p>Don't have an account yet</p--> 
        </form>
        <!--/td>
    </tr>
</table-->
</div>
</div>
{% endblock fos_user_content %}
